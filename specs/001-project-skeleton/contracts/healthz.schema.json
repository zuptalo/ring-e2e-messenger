{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ring.localtest.me/contracts/healthz.schema.json",
  "title": "HealthzResponse",
  "description": "Response body of GET /healthz. Body is intentionally minimal — no diagnostic detail per FR-005.",
  "type": "object",
  "additionalProperties": false,
  "required": ["status"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["ok", "unhealthy"],
      "description": "'ok' iff the backend has verified Postgres connectivity; 'unhealthy' otherwise. No other values are permitted."
    }
  },
  "examples": [
    { "status": "ok" },
    { "status": "unhealthy" }
  ]
}
